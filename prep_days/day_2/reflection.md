# Day 2 — Reflection

Fill in after the timed problems. 1–2 lines per item, no essays.

## Times (actual vs target)

| Problem                          | Target | Actual |
| -------------------------------- | ------ | ------ |
| Top K Frequent                   | 7 min  |        |
| First Unique Character           | 4 min  |        |
| Design HashMap                   | 15 min |        |
| Self-test: top_event_types       | 3 min  |        |

## The one `collections` tool I fumbled

> (Did you reach for `dict.get(k, 0) + 1` when `Counter` was right? Did you forget `most_common(k)` exists? Did you write `if k not in d: d[k] = []` instead of `defaultdict(list)`?)

## The staff-level habit I'd grade myself lowest on today

> (clarify first? brute-force then optimize? modularize? test as you go? narrate trade-offs? **counter-test before declaring done?**)

## One thing I want to drill on Day 3

> (Day 3 is sorting — `sorted()`, custom `key=`, multi-key tuple keys. If something from today is still rough, schedule a 10-min revisit before starting Day 3.)
